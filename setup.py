from setuptools import setup

setup(name='AsciiArt',
      version='0.1',
      description='Ascii "art" from images',
      author='Alyssa',
      packages=['asciiart'],
      install_requires=['pillow', 'requests'])
